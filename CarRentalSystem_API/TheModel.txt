 
 // Model Vehicle
    public class Vehicle:

 public enum VehicleStatus
    {
        Available,
        Rented,
        Maintenance
    }
   
   public enum FuelType
{
    Petrol,
    Diesel,
    Hybrid,
    EV
}

    // model User:
      public enum UserStatus
    {
        Active,
        Inactive,
        Pending,
        Suspended
    }

    //transaction model:
     public enum TransactionType
    {
        InitialPayment,
        ExtensionPayment,
        Refund
    }

    public enum PaymentStatus
    {
        Pending,
        Success,
        Failed
    }

    //model booking
     public enum BookingStatus
    {
        Pending,
        Confirmed,
        Cancelled,
        Completed
    }

    Scrypt.NET

    Car Rental payment processing: booking > add booking db > payment > booking status update > booking end > transaction record > vehicle status update